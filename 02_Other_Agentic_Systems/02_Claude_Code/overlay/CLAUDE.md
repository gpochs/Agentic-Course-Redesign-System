# Course-redesign bridge for Claude Code

@AGENTS.md

Use `.claude/skills/course-redesign/SKILL.md` and its references as the only
course-redesign workflow contract. The project subagents under
`.claude/agents/` are read-only specialist wrappers over those shared
contracts. Do not infer that the presence of these files activates a runtime,
authorizes a gate, permits network access, or grants a write target.

Apply the portable Gate 0A before source discovery. Public availability alone
is not AI-processing authority; route institution-internal/restricted material
without revealing paths, filenames, or content unless this exact environment,
scope, and expiry are approved. Never resume a terminal `complete_dormant` run;
only a fresh trigger creates new lineage, and trigger guidance creates no task
or automation.

Apply the portable lecturer interaction contract through the orchestrator.
Keep one unresolved decision at a time. Claude Code's documented
`AskUserQuestion` contract accepts two to four explicit options; this adapter
requires the documented additional `Other` free-text path. Use that card only
when it contains the complete mutually
exclusive option set. If the control is unavailable or unsupported, or the
complete set has more than four options, ask the same single ordinary-chat
question with every valid numbered option plus `Other - type your answer`, then
wait. Every valid option remains visible. Never prune, hide or combine valid choices merely to fit a
card, and never preselect. Preserve a custom answer verbatim, confirm any normalized mapping,
and do not advance on blank, skip, ambiguity, or uncertainty. Specialist
subagents are evidence lenses, not additional user interfaces. Recap resolved
dependencies before each gate, and keep preliminary criteria/view elicitation
separate from the exact authority gate.

If the import or skill cannot be resolved, state that limitation and read the
project files directly. Never replace the portable safety boundary with a
weaker implicit default.
